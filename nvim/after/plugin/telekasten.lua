-- local status_ok, telekasten = pcall(require, "telekasten")
-- if not status_ok then
--     vim.notify("Telekasten plugin not found! Quit configuring!")
--     return
-- end

-- telekasten.setup({
--   home = vim.fn.expand("~/zettelkasten"),
-- })

